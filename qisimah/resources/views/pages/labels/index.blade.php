@extends('layouts.master')@section('title')    Labels@endsection@section('content')    <div class="page-content container-fluid">        <div class="row">            <a href="{{ route('labels.create') }}" class="btn btn-black" style="margin-bottom: 10px">add label</a>        </div>        <div class="row">            <div class="container">                @if($errors->any())                    <div class="alert alert-danger text-center">                        @foreach($errors->all() as $error)                            <p>{{ $error }}</p>                        @endforeach                    </div>                @elseif(session('error'))                    <div class="alert alert-danger text-center">                        <p>{{ session('error') }}</p>                    </div>                @endif                @if(session('success'))                    <div class="alert alert-success text-center">                        <p>{{ session('success') }}</p>                    </div>                @endif            </div>        </div>        <div class="row table-responsive">            <table class="table table-bordered table-striped">                <thead>                <tr>                    <th>Lable Name</th>                    <th>Actions</th>                </tr>                </thead>                <tbody>                @if($labels)                    @foreach($labels as $label)                        <tr>                            <td>{{ $label->name }}</td>                            <td>                                <div class="row">                                    <a href="{{ route('labels.edit', $label) }}" class="col-sm-3 btn btn-outline                                    btn-primary"><i class="ti-pencil"></i></a>                                    <a href="{{ route('labels.edit', $label) }}" class="col-sm-3 btn btn-outline                                    btn-primary"><i class="ti-pencil"></i></a>                                    <a href="{{ route('labels.edit', $label) }}" class="col-sm-3 btn btn-outline                                    btn-danger"><i class="ti-trash"></i></a>                                    <a href="{{ route('labels.edit', $label) }}" class="col-sm-3 btn btn-outline                                    btn-primary"><i class="ti-pencil"></i></a>                                </div>                            </td>                        </tr>                    @endforeach                @else                    <tr>                        <td colspan="2">No Labels added</td>                    </tr>                @endif                </tbody>            </table>        </div>    </div>@endsection